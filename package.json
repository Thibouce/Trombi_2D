{
  "name": "trombi-2d",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Experience immersive : une personne se prend en photo (webcam), sa tete est detouree puis integree dans un panorama 360 des locaux de l'entreprise.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "start": "node --env-file=.env server.js"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
